# Task: Rebuild ALL frontend pages and components from scratch

The frontend pages and components have been deleted. You must recreate them ALL using the shadcn/ui component library. Read CLAUDE.md and .claude/skills/frontend-design/SKILL.md first.

## What exists (DO NOT modify these):
- src/app/layout.tsx — root layout (imports Nav from @/components/ui/nav)
- src/app/globals.css — CSS variables + shadcn @theme inline block
- src/components/ui/button.tsx, input.tsx, label.tsx, card.tsx, textarea.tsx, badge.tsx — shadcn components
- src/lib/ — all backend code (auth, db, models, stripe, etc.)
- src/app/api/ — all API routes
- src/__tests__/ — all tests
- src/lib/seo.ts — generateMetadata() for SEO
- src/components/analytics-provider.tsx, ad-provider.tsx, ad-unit.tsx, structured-data.tsx — infrastructure

## What you must CREATE (all files were deleted):

### 1. src/components/ui/nav.tsx
- Client component with "use client"
- Sticky header with scroll detection (transparent -> solid on scroll)
- Logo: BlogCraft AI with pen icon (SVG)
- Desktop nav: Pricing (ghost button), Dashboard (ghost when logged in), user avatar+email, Logout, Login/Signup when logged out
- Mobile hamburger menu with animated bars
- Use Button variants: ghost for nav links, default for signup CTA, outline for login
- Fetch user from /api/auth/me, logout via POST /api/auth/logout
- Korean labels: use Korean text for buttons where appropriate

### 2. src/components/landing/hero-section.tsx
- Props: headline, subheadline, ctaText, ctaHref, secondaryCtaText, secondaryCtaHref
- Full-width section min-h-[85vh] with gradient background + grid bg pattern
- Decorative glow blobs (accent color)
- Badge for announcement
- Gradient text headline (gradient-text CSS class)
- Button asChild size="lg" for CTAs
- Social proof bar at bottom
- Default ctaText: "무료로 시작하기"

### 3. src/components/landing/feature-grid.tsx
- Props: heading, subheading, features: { icon: string, title: string, description: string }[]
- Full-width section with py-20
- Section heading centered with subtitle
- 3-column responsive grid (1 col mobile, 2 md, 3 lg)
- Each feature: Card with icon, title, description
- Hover effect on cards

### 4. src/components/landing/cta-section.tsx
- Props: heading, description, ctaText, ctaHref
- Gradient background card (accent/purple)
- Button asChild size="lg" for CTA with arrow icon
- Subtext about free start
- Default ctaText: "무료로 시작하기"

### 5. src/components/landing/footer.tsx
- Multi-column footer with product links, account links
- Copyright: BlogCraft AI
- Korean labels

### 6. src/components/landing/pricing-display.tsx
- Simple pricing display component (wraps PricingSection)

### 7. src/components/pricing-section.tsx
- "use client" component
- Props: plans: Plan[]
- Plan type: { name: string, price: string, description?: string, features: string[], priceId: string, highlighted?: boolean }
- Card for each plan
- Highlighted plan with accent border + "Most Popular" badge
- Korean pricing labels
- Button variants for different actions
- Stripe checkout integration (POST /api/payments/checkout)
- isFree and isCustom helper for Korean price strings

### 8. src/components/premium-gate.tsx
- Premium content gating wrapper
- Shows upgrade prompt for free users

### 9. src/app/page.tsx (Landing Page)
- Import HeroSection, FeatureGrid, CtaSection, Footer
- Hero: headline "사진과 메모만으로 블로그 글 완성", subheadline about AI blog writing
- Stats bar with 4 stats: 10,000+ 활성 블로거, 150,000+ 게시물, 10분 평균 작성, 98% 만족도
- How It Works: 3-step process cards (사진 업로드, AI 작성, 발행)
- Feature grid with 6 features: AI 글 작성, SEO 최적화, 수익화 지원, 말투 학습, 이미지 최적화, 분석 대시보드
- CTA section + Footer
- Use generateMetadata from @/lib/seo for metadata export

### 10. src/app/login/page.tsx
- Use Card, CardContent, Input, Label, Button from shadcn
- BlogCraft AI branding at top with pen icon
- Korean text: heading "다시 오셨군요!", subtitle "계정에 로그인하세요"
- Email + password fields with Label and Input
- Error display with icon
- Button for submit with spinner on loading
- useSearchParams for redirect support (wrap in Suspense)
- Cross-link to signup

### 11. src/app/signup/page.tsx
- Same pattern as login but with name field added
- Korean: heading "계정 만들기", subtitle "무료로 시작하세요"
- Terms text at bottom of card
- Cross-link to login

### 12. src/app/dashboard/page.tsx
- Server component (no "use client"), import getCurrentUser from @/lib/auth
- Redirect to /login if not authenticated (use redirect from next/navigation)
- Call destroySession if no user found
- Header with Korean greeting
- 4 stat cards using Card component: 전체 게시물(0), 발행됨(0), 임시저장(0), 현재 플랜(Free)
- Profile card with avatar initial, name, email, join date (Korean formatted)
- Empty state for blog posts: "아직 게시물이 없습니다" with CTA Button
- Use Button asChild for link buttons

### 13. src/app/pricing/page.tsx
- Import PricingSection
- Three plans: Free (₩0), Pro (₩19,900/월), Enterprise (맞춤)
- Korean feature descriptions for each plan
- Use generateMetadata from @/lib/seo for metadata export

## CRITICAL RULES:
1. EVERY button must use Button from @/components/ui/button
2. EVERY input must use Input from @/components/ui/input
3. EVERY label must use Label from @/components/ui/label
4. EVERY card must use Card from @/components/ui/card
5. Link buttons: Button asChild with Link inside, add className="no-underline" on the Link
6. Use cn() from @/lib/utils for conditional classes
7. All colors via CSS variables: var(--bg), var(--text), var(--accent), var(--border), etc.
8. Layout: full-width sections + max-w-6xl mx-auto px-4 md:px-6 lg:px-8 inner containers
9. Korean text throughout (this is a Korean app, lang="ko")
10. "Linear meets Notion" aesthetic: generous whitespace, clear hierarchy, subtle depth
11. Do NOT modify any existing files (layout.tsx, globals.css, shadcn components, lib/, api/, tests)

## AFTER creating all files:
1. Run: pnpm typecheck — fix ALL errors
2. Run: npx next build --experimental-app-only — must pass
